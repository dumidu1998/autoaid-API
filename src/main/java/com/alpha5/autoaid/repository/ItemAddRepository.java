package com.alpha5.autoaid.repository;

import com.alpha5.autoaid.model.ItemAdd;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ItemAddRepository extends JpaRepository<ItemAdd,Long> {
}
